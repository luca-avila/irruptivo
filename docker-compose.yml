services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: irruptivo
      POSTGRES_PASSWORD: irruptivo
      POSTGRES_DB: irruptivo
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
